"""
utils/__init__.py
------------------
Marks the utils/ directory as a Python package.

Utility modules:
    validators    — JSON Schema validation helpers
    logging_utils — structured terminal logging helpers
    path_utils    — workspace sandbox enforcement
"""
